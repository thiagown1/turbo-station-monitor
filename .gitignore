# ─── Dependencies ───
node_modules/

# ─── Databases (runtime data, ~10GB+) ───
db/*.db
db/*.db-shm
db/*.db-wal
db/backups/
# Stale .db files that ended up in root
*.db

# ─── Logs (PM2 output) ───
logs/

# ─── Runtime state files ───
history/*.json
github-webhook-queue.jsonl
github-webhook-queue.jsonl.bak
github-ack-debounce.json
ci-fix-attempts.json
last-report.json

# ─── Backup files ───
*.backup
*.backup2
*.bak
*.bak.*
*.patch

# ─── Old phase completion reports (not needed in repo) ───
COMPLETION_REPORT.txt
PHASE3_COMPLETION_REPORT.txt
PHASE3_SUMMARY.md
PHASE4_COMPLETE.md
PHASE4_HANDOFF.md
PHASE4_QUICKREF.txt

# ─── Partner alert config (gitignored — contains partner names/IDs) ───
config/partner-alert-stations.json

# ─── Environment ───
.env
.env.local
.env.production

# ─── OS / Editor ───
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
# ─── auto-rollback watchdog (shadow mode) runtime state + kill-switch flags ───
deploy-rollback-state.json
auto-rollback.enabled
auto-rollback.disabled
logs/auto-rollback-decisions.log

# --- VPS runtime state / secrets / backups — never commit (reconciled 2026-07-09) ---
services/whatsapp-gateway/auth/
*.sqlite
*.sqlite-shm
*.sqlite-wal
.env.bak*
*.bak-*
**/.bak-*/
data/
db/
state/
tmp/
.deploy-monitor-state.json
.deploy_monitor_state.json
.vercel_deploy_monitor_state.json
.vercel_drain_monitor_state.json
.deploy-hook-last-prod-sha
ci-pending-wakes.json
deploy-autowatch.state
deploy-monitor-state.json
deploy-monitor-state.txt
deploy_monitor_state.json
.cbmignore
services/turbo-parking/
